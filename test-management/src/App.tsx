import type {ReactNode} from 'react';
import {BrowserRouter,Routes,Route,Navigate} from 'react-router-dom'; import Layout from './components/Layout'; import Login from './pages/Login'; import Dashboard from './pages/Dashboard'; import TestForm from './pages/TestForm'; import Questions from './pages/Questions'; import Preview from './pages/Preview'; import {useAuth} from './store/auth'; import './styles.css';
function Protected({children}:{children:ReactNode}){return useAuth(s=>s.token)?children:<Navigate to="/login" replace/>}
export default function App(){return <BrowserRouter><Routes><Route path="/login" element={<Login/>}/><Route element={<Protected><Layout/></Protected>}><Route path="/dashboard" element={<Dashboard/>}/><Route path="/tests/new" element={<TestForm/>}/><Route path="/tests/:id/edit" element={<TestForm/>}/><Route path="/tests/:id/questions" element={<Questions/>}/><Route path="/tests/:id/preview" element={<Preview/>}/></Route><Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes></BrowserRouter>}
