import {Outlet,NavLink,useNavigate} from 'react-router-dom'; import {ClipboardList,LayoutDashboard,LogOut,Plus,UserCircle} from 'lucide-react'; import {useAuth} from '../store/auth';
export default function Layout(){const nav=useNavigate();const {user,logout}=useAuth();return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark">P</span><div><b>Preproute</b><small>Test Manager</small></div></div><nav><NavLink to="/dashboard"><LayoutDashboard/>Dashboard</NavLink><NavLink to="/tests/new"><Plus/>Create Test</NavLink></nav><div className="side-bottom"><div className="user-mini"><UserCircle/><div><strong>{String(user?.name||user?.userId||'Admin')}</strong><small>Administrator</small></div></div><button className="logout" onClick={()=>{logout();nav('/login')}}><LogOut/>Sign out</button></div></aside><main className="main"><header className="topbar"><div><span className="crumb"><ClipboardList/> Test Management</span></div><div className="top-user"><span>Admin workspace</span><div className="avatar">{String(user?.name||user?.userId||'A').charAt(0).toUpperCase()}</div></div></header><div className="content"><Outlet/></div></main></div>}
