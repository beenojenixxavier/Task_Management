import {FormEvent,useState} from 'react'; import {Navigate,useNavigate} from 'react-router-dom'; import {Eye,EyeOff,LockKeyhole,LogIn,ShieldCheck,UserRound} from 'lucide-react'; import {authApi,getError} from '../lib/api'; import {useAuth} from '../store/auth';
export default function Login(){const {token,setSession}=useAuth();const nav=useNavigate();const [userId,setUserId]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [loading,setLoading]=useState(false);const [error,setError]=useState(''); if(token)return <Navigate to="/dashboard" replace/>; const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(!userId.trim()||!password){setError('Enter both user ID and password.');return}try{setLoading(true);const s=await authApi.login(userId.trim(),password);setSession(s.token,s.user);nav('/dashboard')}catch(err){setError(getError(err)||'Login failed. Please check your credentials.')}finally{setLoading(false)}};return <div className="login-page"><div className="login-art"><div className="login-brand"><span className="brand-mark">P</span><b>Preproute</b></div><div className="art-copy"><span className="eyebrow">TEST MANAGEMENT</span><h1>Build better tests.<br/><em>Measure better.</em></h1><p>Create, organise, review and publish assessments from one focused workspace.</p><div className="feature-row"><div><ShieldCheck/><span>Secure workspace</span></div><div><LockKeyhole/><span>JWT protected</span></div></div></div><div className="art-footer">© 2026 Preproute</div></div><div className="login-panel"><div className="login-card"><div className="mobile-logo"><span className="brand-mark">P</span><b>Preproute</b></div><span className="eyebrow">WELCOME BACK</span><h2>Sign in to your workspace</h2><p className="muted">Use your administrator credentials to continue.</p><form onSubmit={submit}><label>User ID<div className="input-icon"><UserRound/><input value={userId} onChange={e=>setUserId(e.target.value)} placeholder="Enter user ID" autoComplete="username"/></div></label><label>Password<div className="input-icon"><LockKeyhole/><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter password" autoComplete="current-password"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="form-error">{error}</div>}<button className="primary full" disabled={loading}>{loading?'Signing in…':<><LogIn/>Sign in</>}</button></form><p className="login-note">Authorised access only</p></div></div></div>}
